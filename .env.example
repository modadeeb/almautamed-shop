# انسخ هذا الملف إلى .env.local

# قاعدة عنوان الـ API لنظام ERP (Laravel + Sanctum). لن يُستخدم في المرحلة 1 (mock فقط).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/store

# العملة الافتراضية للعرض. تُقرأ عبر formatMoney ولا تُكتب أي رموز عملة ثابتة في الكود.
# TODO: العملة تُحسم لاحقاً (تأتي من الإعدادات/الـ API حسب نوع العميل والسوق).
NEXT_PUBLIC_DEFAULT_CURRENCY=YER
