import { PagePlaceholder } from "@/components/page-placeholder";

export default function LoginPage() {
  return (
    <PagePlaceholder
      title="تسجيل الدخول"
      description="نموذج تسجيل الدخول — قيد الإنشاء."
    />
  );
}
